Replace the jar files by a makefile generating them. (RiskAI.jar, RiskClient.jar, RiskServer.jar); add *.jar to gitignore - upgrade to gradle, and provide a wrapper
Update the README to reflect changes to use Gradle
Sort out the unsorted folder
Start use the github issue tracker for the project instead of this TODO file
Change config so gameLog.txt is created in a more sensible place than project root (and is timestamped?)
Resolve licencing issues with third-party code used in this project
